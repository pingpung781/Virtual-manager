from app.core.logging import logger

def decompose_goal(goal: str) -> list[str]:
    # Placeholder for LLM decomposition logic
    logger.info(f"Decomposing goal: {goal}")
    steps = [f"Step 1 for {goal}", f"Step 2 for {goal}"]
    return steps
